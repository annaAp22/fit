$(function() {
    window.initDefer({
        url: window.routes.product_defer_path,
        buttonSelector: '.defer-product',
        activeClass: 'active',
        countSelector: '.mod-bookmarks span'
    });
});